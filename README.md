# private-transfers-benchmarks

A set of adapters to run structured benchmarks for private transfers protocols

## Structure

- `gas-benchmarks`: Private transfer protocol gas benchmarks. Benchmark data is retrieved from onchain transactions via RPC.

- `project-evaluations`: Evaluations of private transfer protocols based on various criteria. Evaluations are stored in a JSON file and can be used to generate reports or visualizations.
