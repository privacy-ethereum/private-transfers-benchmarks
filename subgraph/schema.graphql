type HinkalProtocolStats @entity(immutable: false) {
  id: ID!
  totalTxCount: BigInt!
  totalGasUsed: BigInt!

  shieldNative: HinkalOperationStats!
  unshieldNative: HinkalOperationStats!
  shieldERC20: HinkalOperationStats!
  unshieldERC20: HinkalOperationStats!
  transact: HinkalOperationStats!
}

type HinkalShieldERC20TokenStats @entity(immutable: false) {
  id: ID!
  tokenAddress: Bytes!
  totalCount: BigInt!
  totalGasUsed: BigInt!
  totalValue: BigInt!

  operationStats: HinkalOperationStats!
}

type HinkalUnshieldERC20TokenStats @entity(immutable: false) {
  id: ID!
  tokenAddress: Bytes!
  totalCount: BigInt!
  totalGasUsed: BigInt!
  totalValue: BigInt!

  operationStats: HinkalOperationStats!
}

type HinkalShieldNativeTokenStats @entity(immutable: false) {
  id: ID!
  totalCount: BigInt!
  totalGasUsed: BigInt!

  operationStats: HinkalOperationStats!
}

type HinkalUnshieldNativeTokenStats @entity(immutable: false) {
  id: ID!
  totalCount: BigInt!
  totalGasUsed: BigInt!

  operationStats: HinkalOperationStats!
}

type HinkalTransactTokenStats @entity(immutable: false) {
  id: ID!
  totalCount: BigInt!
  totalGasUsed: BigInt!

  operationStats: HinkalOperationStats!
}

type HinkalOperationStats @entity(immutable: false) {
  id: ID!
  totalCount: BigInt!
  totalGasUsed: BigInt!

  shieldERC20TokenStats: [HinkalShieldERC20TokenStats!]! @derivedFrom(field: "operationStats")
  unshieldERC20TokenStats: [HinkalUnshieldERC20TokenStats!]! @derivedFrom(field: "operationStats")
  shieldNativeTokenStats: [HinkalShieldNativeTokenStats!]! @derivedFrom(field: "operationStats")
  unshieldNativeTokenStats: [HinkalUnshieldNativeTokenStats!]! @derivedFrom(field: "operationStats")
  transactTokenStats: [HinkalTransactTokenStats!]! @derivedFrom(field: "operationStats")
}

type HinkalTransferLog @entity(immutable: true) {
  id: Bytes!
  from: Bytes!
  to: Bytes!
  amount: BigInt!
  address: Bytes!
}

type HinkalShieldNative @entity(immutable: true) {
  id: ID!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type HinkalTransact @entity(immutable: true) {
  id: ID!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type HinkalShieldERC20 @entity(immutable: true) {
  id: ID!
  tokenAddress: Bytes!
  value: BigInt!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type HinkalUnshieldNative @entity(immutable: true) {
  id: ID!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type HinkalUnshieldERC20 @entity(immutable: true) {
  id: ID!
  tokenAddress: Bytes!
  value: BigInt!
  fee: BigInt!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type PrivacyPoolsProtocolStats @entity(immutable: false) {
  id: ID!
  totalTxCount: BigInt!
  totalGasUsed: BigInt!

  shield: PrivacyPoolsOperationStats!
  unshield: PrivacyPoolsOperationStats!
}

type PrivacyPoolsPool @entity(immutable: true) {
  id: ID!
  pool: Bytes!
  asset: Bytes!
}

type PrivacyPoolsOperationStats @entity(immutable: false) {
  id: ID!
  totalCount: BigInt!
  totalGasUsed: BigInt!
  totalValue: BigInt!

  shieldTokenStats: [PrivacyPoolsShieldStats!]! @derivedFrom(field: "operationStats")
  unshieldTokenStats: [PrivacyPoolsUnshieldStats!]! @derivedFrom(field: "operationStats")
}

type PrivacyPoolsShieldStats @entity(immutable: false) {
  id: ID!
  tokenAddress: Bytes!
  totalCount: BigInt!
  totalGasUsed: BigInt!
  totalValue: BigInt!

  operationStats: PrivacyPoolsOperationStats!
}

type PrivacyPoolsUnshieldStats @entity(immutable: false) {
  id: ID!
  tokenAddress: Bytes!
  totalCount: BigInt!
  totalGasUsed: BigInt!
  totalValue: BigInt!

  operationStats: PrivacyPoolsOperationStats!
}

type PrivacyPoolsDeposit @entity(immutable: true) {
  id: ID!
  tokenAddress: Bytes!
  amount: BigInt!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type PrivacyPoolsWithdrawal @entity(immutable: true) {
  id: ID!
  tokenAddress: Bytes!
  fee: BigInt!
  amount: BigInt!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type RailgunProtocolStats @entity(immutable: false) {
  id: ID!
  totalTxCount: BigInt!
  totalGasUsed: BigInt!

  shield: RailgunOperationStats!
  unshield: RailgunOperationStats!
  transact: RailgunOperationStats!
}

type RailgunShieldTokenStats @entity(immutable: false) {
  id: ID!
  tokenAddress: Bytes!
  totalCount: BigInt!
  totalGasUsed: BigInt!
  totalValue: BigInt!

  operationStats: RailgunOperationStats!
}

type RailgunUnshieldTokenStats @entity(immutable: false) {
  id: ID!
  tokenAddress: Bytes!
  totalCount: BigInt!
  totalGasUsed: BigInt!
  totalValue: BigInt!

  operationStats: RailgunOperationStats!
}

type RailgunOperationStats @entity(immutable: false) {
  id: ID!
  totalCount: BigInt!
  totalGasUsed: BigInt!

  shieldTokenStats: [RailgunShieldTokenStats!]! @derivedFrom(field: "operationStats")
  unshieldTokenStats: [RailgunUnshieldTokenStats!]! @derivedFrom(field: "operationStats")
}

type RailgunShield @entity(immutable: true) {
  id: ID!
  treeNumber: BigInt!
  startPosition: BigInt!
  fees: [BigInt!]!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type RailgunUnshield @entity(immutable: true) {
  id: ID!
  to: Bytes!

  tokenType: Int!
  tokenAddress: Bytes!
  tokenSubID: BigInt!

  amount: BigInt!
  fee: BigInt!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type RailgunTransact @entity(immutable: true) {
  id: ID!
  treeNumber: BigInt!
  startPosition: BigInt!
  hash: [Bytes!]!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type RedactProtocolStats @entity(immutable: false) {
  id: ID!
  totalTxCount: BigInt!
  totalGasUsed: BigInt!

  shieldedNative: RedactOperationStats!
  unshielded: RedactOperationStats!
}

type RedactOperationStats @entity(immutable: false) {
  id: ID!
  totalCount: BigInt!
  totalGasUsed: BigInt!

  shieldedNativeStats: [RedactShieldedStats!]! @derivedFrom(field: "operationStats")
  unshieldedStats: [RedactUnshieldedStats!]! @derivedFrom(field: "operationStats")
}

type RedactShieldedStats @entity(immutable: false) {
  id: ID!
  tokenAddress: Bytes
  totalCount: BigInt!
  totalGasUsed: BigInt!
  totalValue: BigInt!

  operationStats: RedactOperationStats!
}

type RedactUnshieldedStats @entity(immutable: false) {
  id: ID!
  totalCount: BigInt!
  totalGasUsed: BigInt!
  totalValue: BigInt!

  operationStats: RedactOperationStats!
}

type RedactShieldedNative @entity(immutable: true) {
  id: ID!
  from: Bytes!
  to: Bytes!
  value: BigInt!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type RedactUnshielded @entity(immutable: true) {
  id: ID!
  to: Bytes!
  amount: Bytes!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type TornadoCashProtocolStats @entity(immutable: false) {
  id: ID!
  totalTxCount: BigInt!
  totalGasUsed: BigInt!

  shield: TornadoCashOperationStats!
  unshield: TornadoCashOperationStats!
}

type TornadoCashOperationStats @entity(immutable: false) {
  id: ID!
  totalCount: BigInt!
  totalGasUsed: BigInt!
  totalValue: BigInt!
}

type TornadoCashInstance @entity(immutable: false) {
  id: ID!
  address: Bytes!
  active: Boolean!
}

type TornadoCashDeposit @entity(immutable: true) {
  id: ID!
  amount: BigInt!
  instance: TornadoCashInstance!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}

type TornadoCashWithdrawal @entity(immutable: true) {
  id: ID!
  fee: BigInt!
  amount: BigInt!
  instance: TornadoCashInstance!

  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
  gasUsed: BigInt!
  gasPrice: BigInt!
}
